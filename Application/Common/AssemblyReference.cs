namespace DecoApp.Application.Common
{
    public static class AssemblyReference
    {
        // Esta clase se utiliza como punto de referencia para obtener el assembly de esta capa.
        // Permite que otras capas registren automáticamente servicios, handlers, validators, etc.,
        // sin depender de clases concretas y evitando dependencias circulares.
    }
}